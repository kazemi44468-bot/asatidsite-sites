(()=>{const root=document.querySelector('.intro');if(!root)return;const track=root.querySelector('.intro-track'),slides=[...root.querySelectorAll('.intro-slide')],dots=[...root.querySelectorAll('.intro-dot')],prev=root.querySelector('[data-intro-prev]'),next=root.querySelector('[data-intro-next]'),counter=root.querySelector('.intro-current'),portrait=root.querySelector('.portrait-main'),portraitThumbs=[...root.querySelectorAll('.portrait-thumb')];let index=0,portraitIndex=0,startX=0,deltaX=0,dragging=false,timer,portraitTimer;const portraitUrls=['https://images.unsplash.com/photo-1560250097-0b93528c311a?auto=format&fit=crop&w=900&q=88','https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=900&q=88','https://images.unsplash.com/photo-1519085360753-af0119f7cbe7?auto=format&fit=crop&w=900&q=88'];const render=()=>{track.style.transform=`translate3d(${index*-100}%,0,0)`;dots.forEach((d,i)=>d.classList.toggle('active',i===index));if(counter)counter.textContent=String(index+1).padStart(2,'0')};const restart=()=>{clearInterval(timer);timer=setInterval(()=>go(index+1),6200)};const go=i=>{index=(i+slides.length)%slides.length;render();restart()};const renderPortrait=()=>{if(portrait)portrait.style.backgroundImage=`url('${portraitUrls[portraitIndex]}')`;portraitThumbs.forEach((t,i)=>t.classList.toggle('active',i===portraitIndex))};const restartPortrait=()=>{clearInterval(portraitTimer);portraitTimer=setInterval(()=>{portraitIndex=(portraitIndex+1)%portraitUrls.length;renderPortrait()},7600)};prev?.addEventListener('click',()=>go(index-1));next?.addEventListener('click',()=>go(index+1));dots.forEach((d,i)=>d.addEventListener('click',()=>go(i)));portraitThumbs.forEach((t,i)=>t.addEventListener('click',()=>{portraitIndex=i;renderPortrait();restartPortrait()}));track.addEventListener('pointerdown',e=>{dragging=true;startX=e.clientX;deltaX=0;track.setPointerCapture?.(e.pointerId)});track.addEventListener('pointermove',e=>{if(dragging)deltaX=e.clientX-startX});const end=()=>{if(!dragging)return;dragging=false;if(Math.abs(deltaX)>55)go(index+(deltaX<0?1:-1));deltaX=0};track.addEventListener('pointerup',end);track.addEventListener('pointercancel',end);root.addEventListener('keydown',e=>{if(e.key==='ArrowLeft')go(index-1);if(e.key==='ArrowRight')go(index+1)});render();renderPortrait();restart();restartPortrait()})();